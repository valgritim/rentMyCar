{% extends 'base.html.twig' %}

{% block title%}
    {{ advert.title }}
{% endblock %}
{% block body %}
    
        <div class="advertTitle" style="background-image:url({{advert.coverImage}})">
            <div class="container">
                <h1>{{ advert.title }}</h1>
                <p>{{ advert.introduction }}</p>
                <p class="h2">
                    <strong>{{ advert.seats }} places</strong> pour <strong>{{ advert.price}} Euros</strong> par jour
                </p>
                <a href="#" class="btn btn-primary">Réserver!</a>
            </div>    
        </div>
    
    <div class="container">
        <div class="row">
            <div class="col-md-8 mb-3">
                {{ advert.content | raw }}
                <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
                    <ol class="carousel-indicators">
                        {% for image in advert.advImages %}
                            <li data-target="#carouselExampleIndicators" data-slide-to="{{ loop.index0 }}" {% if loop.first %}class="active"{% endif %}></li>
                        {% endfor %}
                        
                    </ol>
                    <div class="carousel-inner">
                        {% for image in advert.advImages %}
                        <div class="carousel-item {% if loop.first %}active {% endif %}">
                            <img src="{{ image.url }}" class="d-block w-100" alt="...">
                            <div class="carousel-caption d-none d-md-block">
                                <h5>{{ advert.title }}</h5>
                                <p>{{ image.caption }}</p>
                            </div>
                        </div>
                        {% endfor %}
                    </div>
                       
                   
                    <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="sr-only">Next</span>
                    </a>
                </div>
            </div>
            <div class="col">
                <div class="row mb-3 align-items-center">
                    <div class="col-4">
                        <img src="http://placehold.it/64x64" alt="photo utilisateur">
                    </div>
                    <div class="col">
                        <h3>Joseph Dupont</h3>
                        <span class="badge badge-primary">3 annonces</span>
                    </div>
                </div>
                <p>Lorem ipsum dolor, sit amet consectetur adipisicing elit. Atque nulla expedita maiores, fugit vel est non repudiandae totam? Sed, corporis.</p>
                <p>Accusamus odio ipsa suscipit sequi aut id neque unde voluptas ratione cumque accusantium dicta explicabo laboriosam culpa nemo, exercitationem dignissimos.</p>
                <p>Deleniti rerum, nesciunt quibusdam tempore natus tempora consequuntur architecto fuga suscipit quo! Alias accusantium dolorum reiciendis architecto et ex sequi?</p>
            </div>       
        </div>
    
    
    </div>
    


{% endblock %}